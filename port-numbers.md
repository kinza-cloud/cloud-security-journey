# Port Numbers

A port number tells the data where it should go. It is always used with an IP address.

## Common port numbers:
- Port 80 → HTTP (normal websites)
- Port 443 → HTTPS (secure websites)
- Port 22 → SSH (remote access to servers)

## One sentence to remember:
IP address finds the device. Port number finds the service on that device.
